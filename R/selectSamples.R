#' @export selectSamples

selectSamples <- function(
    experiment = NULL,
    samples = NULL, # vector of sample names to be selected
    variable = NULL, #
    level = NULL
){
  result <- new_mgmisc1()



  return(result)
}
